<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liman.www.manage.mapper.PmsSkuImageMapper">

    <insert id="insert" useGeneratedKeys="true">
        insert into pms_sku_image (sku_id, img_name, img_url, product_img_id, is_default) values
        <foreach collection="param1" item="pmsSkuImage" separator=",">
            (#{param2},#{pmsSkuImage.imgName},#{pmsSkuImage.imgUrl},#{pmsSkuImage.productImgId},#{pmsSkuImage.isDefault})
        </foreach>
    </insert>
</mapper>